{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum Tauri IPC surface for the KAIRO main window. Local device access is exposed only through KAIRO-owned bounded commands.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
